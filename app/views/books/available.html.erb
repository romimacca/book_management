<h1>Available Books</h1>

<% if @books.any? %>
    <div id="books_list" class="row">
        <%= render @books %>
    </div>
<% else %>
    <div class="alert alert-secondary" role="alert">
        Now we don't have books available
    </div>
<% end %>